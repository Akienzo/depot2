<div class="cart_title">カート</div>
<table>
  <%= render(cart.line_items) %>

  <tr class="total_line">
    <td colspan="2">合計</td>
    <td class="total_cell"><%= cart.total_price %>円</td>
  </tr>

</table>

<%= button_to 'カートを空にする', cart, method: :delete,
    confirm: '本当によいですか？' %>